import React from 'react';
import PasswordSetupOtp from '../../component/features/Authenticate/passwordSetup/PasswordSetupOtp';

const SignupPage = () => {
  return <PasswordSetupOtp />;
};

export default SignupPage;
